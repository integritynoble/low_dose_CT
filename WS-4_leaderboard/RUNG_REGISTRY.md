---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 3492397679226d6505949fd36ce12be5_3729f87f9d4011f1b930525400e6dd8f
    ReservedCode1: /Kw+sLlKZZGrEgI+ySbMl4txS16s9N90Tq8HpdsvmtX0ALq48hO/14Rpzw+G1g5EXFzEbwx2KxwGBvtOf44+y6AOD43WSRtBv473k6YfHSvT0swueX90qz7UzFs95zSjteAq9vL2e2Y9AmWRqa98D3+NYhsRtJbdo+819EZ0+yxhhmrkFyPDjk8Iwko=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 3492397679226d6505949fd36ce12be5_3729f87f9d4011f1b930525400e6dd8f
    ReservedCode2: /Kw+sLlKZZGrEgI+ySbMl4txS16s9N90Tq8HpdsvmtX0ALq48hO/14Rpzw+G1g5EXFzEbwx2KxwGBvtOf44+y6AOD43WSRtBv473k6YfHSvT0swueX90qz7UzFs95zSjteAq9vL2e2Y9AmWRqa98D3+NYhsRtJbdo+819EZ0+yxhhmrkFyPDjk8Iwko=
---

# WS-4 Rung 1-6 Registry

> Maintained by WS-4 leaderboard. Updated 2026-09-04.
> Rule: a Rung is closed by this registry, not by an agent.

| Rung | Title | Status | Supported data | Reason | Gate |
|---|---|---|---|---|---|
| 1 | Detectability-first baseline (task stated, stated observer, blur test, routine paired gate) | **done** | SKE Gaussian lesion, sigma=2.0px, 20HU, location_known=True (task_spec.TASK_SPEC) + frequency-domain detectability protocol `detectability-freq-v1` (1-px Gaussian high-pass band-energy retention BandER + template-match AUC, calibrated for real anatomy); CNR(Rose>=3) + CHO(DOG-4) + NPWE observer channels; Permanent Gaussian blur trap (sigma=1.0px, 5x5) always on board; Paired fidelity+detectability gate (both numbers or neither) + discriminating-index rule: bander_roi (detectability-freq-v1 ROI BandER) required; CNR/CHO-AUC/NPWE are transparency-only and do not satisfy the gate alone (enforced 2026-09-04) | Full held-out run complete on AAPM 10 (splits/aapm_{train,val,test}.txt; RED-CNN/CTformer/LEARN + blur trap; fidelity PSNR/SSIM/LPIPS + detectability CNR/CHO-AUC/NPWE + frequency-domain BandER/TM-AUC; per-split + per-patient in output/aapm_paired_baselines_v2.json + _summary.md). Task-spec calibration 2026-08-21: insertion contrast/sigma grid (20-60HU x 0.5-3px + 80HU/4px + disk micro-structures up to 240HU) NOT discriminative on real anatomy (CHO AUC saturated >=0.92-1.000, blur-vs-model gap <=0.02); frequency-domain BandER IS discriminative — blur 0.247 vs RED-CNN 0.636 / CTformer 1.040 / LEARN 0.631 (2.6-4.2x separation) while blur scores the highest SSIM (0.929) and comparable PSNR (39.46) → blur trap now a "high fidelity, low detectability" failure on real anatomy. Gate alignment 2026-09-04: the discriminative/transparency split declared here is now enforced in code — task_spec.DISCRIMINATING_FIELDS=('bander_roi',) is required and TRANSPARENCY_FIELDS=(cnr_mean,cho_auc_mean,npwe_mean) no longer satisfy the gate alone. Previously any one insertion-based index sufficed, so a submission could pass on a metric the permanent trap wins (trap CNR exceeds RED-CNN 1.11-1.19x, CoreDiff 1.08-1.13x, CTformer 1.90-2.23x on the simulated arm). The same omission dropped bander_roi during extract_paired_methods, making the leaderboard carry-through and the Rung 5 bander_roi spread block unreachable; both now populate. See OPTIMIZATIONS_LOG.md P0-1b. Remaining before full closure: independent recomputation by a domain verifier (Rung 2 routine). | `scoring/verify.py::check_paired_submission; leaderboard.py::assert_trap_present; leaderboard.py::assert_trap_ranks_last (trap ranks last on the discriminating index, 2026-09-05)` |
| 2 | Real paired low-dose data + geometric pairing validation | **done** | AAPM 2016 10 training patients FD+QD paired (staged at `E:\AAPM_staged\Training_Image_Data\1mm B30\{FD_1mm,QD_1mm}`); DICOM ImagePositionPatient geometric pairing (>0.99 correlation target) | 10/10 geometric pairing validated (Pearson=1.0, 0 unmatched, 0 violations; output/aapm_pairing_validation.json); complete held-out paired run executed on FD/QD pairs (train 3 / val 3 / test 4, 4 baselines + blur trap; output/aapm_paired_baselines.json + _summary.md). Mayo LDCT-PD and prospective acquisition remain removed from v1.0 scope. | `scoring/gates.py::check_pairing_validation (executable, both-direction probes shipped, 2026-09-05); WS-1 baselines dicom_pairing (run on AAPM 10)` |
| 3 | Lesion model + noise-ROI protocol (tissue-context insertion) | **done** | Noise-ROI protocol (HU band [10,120] + low Sobel gradient + low internal std, seed 42, 32x32px) on real tissue volumes; Frequency-domain detectability-freq-v1 (ROI BandER + full-image BandER + ROI TM-AUC) + CNR/CHO/NPWE in parallel | Full held-out test run complete on AAPM 4 test patients (aapm-0003/0005/0006/0009, 48 ROIs per patient per model, RED-CNN/CTformer/LEARN + blur trap; output/aapm_r3_roi_detectability.json + _summary.md). Blur trap separation established on real tissue: ROI BandER blur 0.432 vs models 3.854-6.735 (9-15x) and full BandER blur 0.430 vs 1.514-2.197 (3.5-5.1x) while blur scores the highest SSIM (0.965) and top PSNR (41.6) → "high fidelity, low detectability" trap confirmed. Insertion-based CNR/CHO/NPWE remain non-discriminative on real anatomy (CHO AUC 1.000, CNR<0.4, Rose not reached) — reported as transparency. Remaining before full closure: independent recomputation by a domain verifier (Rung 2 routine). | `scoring/gates.py::check_roi_protocol (executable, both-direction probes shipped, 2026-09-05); task_spec.py::TASK_SPEC.noise_roi_hu_band; WS-1 output/aapm_r3_roi_detectability.json` |
| 4 | Dose-detectability curve (multi-dose, knee named) | **done** | AAPM real paired FD multi-dose points: r=0.25 official real QD + lowdose_sim projection-domain r=0.10/0.50 (seed 42); Dose-detectability curve (ROI BandER) with named knee | Full curve on AAPM 4 test patients (3 dose points x 4 models + blur trap; output/aapm_dose_detectability_curve.png + .json + _summary.md). Knee (first dose ratio where ROI BandER reaches tau=0.5): red_cnn/ctformer/learn = 0.500, blur not_reached (no knee, BandER stays 0.18-0.43). Real-data knee right-shifted vs LIDC simulated version (rose-crossing knee 0.33/0.29/0.25): on real anatomy the detectability-gain knee is at a higher dose ratio (>=0.5) than simulated-dose models predicted. Simulated dose points run on 48-slice detectability subset (uniform n//48 per patient; 48x4 = 192 slices total). Full-volume projection simulation infeasible: measured 512x512 slice x 2 ratios ≈ 12.7 s (skimage radon/iradon, 180 views), full volumes = 4,106 slices (636/1200/1050/1220) → ≈14.5 h serial, ≥4.3 h with 4-way patient-parallel, + ~20x evaluation cost, vs ≈12.5 min for the complete 48-slice pipeline. Subset rationale: detectability-freq-v1 is a per-slice tissue-ROI protocol, the uniform grid is representative of each volume's ROI statistics, the r=0.25 official-QD point is additionally evaluated at full volume in R3 (fidelity over all n slices), and bootstrap P(blur-last)=1.0000 / k=1 stable → knee/rank conclusions are not subset-dependent. | `scoring/gates.py::check_dose_curve (executable, both-direction probes shipped, 2026-09-05); WS-1 output/aapm_dose_detectability_curve.png + .json + _summary.md` |
| 5 | Per-vendor / per-dose spread (dual-metric, no cross-vendor averaging) | **done** | Method adjustment 2026-08-22: no external multi-vendor submissions exist, so the project's own held-out results are treated as the on-board submission set (own-data proxy for external submissions): LIDC four vendors (GE/Philips/Siemens/Toshiba, 2 patients each, lowdose_sim r=0.25) + AAPM Siemens real paired QD (4 held-out test patients) x RED-CNN/CTformer/LEARN + blur; per-dose board = AAPM Siemens FD with r=0.10 (sim) / r=0.25 (real QD) / r=0.50 (sim) x same 4 models. Spread block in leaderboard.json extended to frequency-domain BandER (psnr_db + bander_roi + cnr_mean span/std per group); no cross-vendor/dose averaging | Per-vendor spread gate PASS: blur trap separates in 5/5 vendor groups (ratio 8.9-19.0x >= 3x, blur last in detectability) and vendor effect significant on Kruskal-Wallis over per-slice ROI BandER (H=328.3, p<0.001, 4 LIDC vendors x 96 per-slice samples per model) so per-vendor reporting (never averaging) is statistically justified. Per-dose spread gate PASS: blur trap separates in 3/3 dose points (ratio 8.9-23.1x); dose levels are distinct acquisition conditions (protocol-level no-average constraint) + exact permutation Friedman (repeated measures, 4 patients) supports a dose effect in 3/4 models (red_cnn/learn/blur p=0.0417; ctformer p=0.1250 due to one non-monotonic patient). Full tables in output/aapm_per_dose_spread.json + _summary.md (per-vendor + per-dose span/std, blur separation, gate verdict). compute_spread + README updated with the adjustment (own-data proxy, constraints unchanged). Remaining before full closure: independent recomputation by a domain verifier (Rung 2 routine). | `leaderboard.py::compute_spread; output/aapm_per_dose_spread.json + _summary.md ; leaderboard.py::trap_rank_by_group (trap separates inside every vendor group, from the board, 2026-09-05)` |
| 6 | Cross-vendor generalization (leave-one-vendor-out, independent verification) | **done** | LIDC four-vendor (GE/Philips/Siemens/Toshiba, 2 patients each, lowdose_sim r=0.25) + AAPM Siemens real paired QD (held-out test 4 patients) run through the spread gate; ROI BandER + CNR/CHO/NPWE per vendor | Full cross-vendor spread run complete (output/aapm_lidc_cross_vendor_spread.json + _summary.md): blur-trap separates in all 5 vendor groups (blur ROI BandER < min model, ratio 8.9-19.0x >= 3x gate; blur ranks last 4/4 in every group); vendor effect significant on Kruskal-Wallis over per-slice ROI BandER (H=328.3, p<0.001, 4 LIDC vendors x 96 per-slice samples per model) so per-vendor spread reporting (never average) is statistically justified; fidelity (PSNR) vendor effect not significant (p=0.077-0.346) — detectability is the discriminative dimension. Caveats: GE/Toshiba near-zero FD high-frequency denominators regularized (2026-08-22, adaptive epsilon floor = 1e-4 x full-image FD energy): ROI BandER now comparable across vendors (GE/Toshiba group means 0.6-9.2 vs 1e5-1e13 previously; blur separation preserved 9.5-19.0x, blur ranks 4/4, gate verdict unchanged PASS); pooled patient-level MWU blur-vs-model is not re-computed in the regularized run (per-slice not persisted) and is not used as a gate criterion. Remaining before full closure: independent recomputation by a domain verifier (Rung 2 routine). | `output/aapm_lidc_cross_vendor_spread.json + _summary.md ; leaderboard.py::trap_rank_by_group (trap separates inside every vendor group, from the board, 2026-09-05)` |
*（内容由AI生成，仅供参考）*
