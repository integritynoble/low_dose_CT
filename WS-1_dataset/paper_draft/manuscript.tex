%% =====================================================================
%% PWM-LDCT v0.5 Dataset Paper — Nature Scientific Data submission
%%
%% Target venue: Nature Scientific Data (Data Descriptor format)
%% Submission target: D9 + 180 (~6 months post-mainnet)
%%
%% Strategy: v0.5 ships first using ONLY public data (LIDC-IDRI +
%% AAPM 2016 + Mayo LDCT-PD) under a unified content-addressed
%% harmonization schema. v1.0 (with prospective multi-vendor UTSW +
%% partner-site acquisitions) is a separate follow-up release; its
%% manuscript draft is preserved at manuscript_v1.tex.
%%
%% Status: v0.5 working draft. RESULTS sections marked with \todo{} are
%%         placeholders to fill as the v0.5 harmonization pipeline +
%%         baseline reproductions + AAPM access land in the first
%%         ~6 months.
%%
%% Build:  pdflatex manuscript && bibtex manuscript && pdflatex manuscript
%%         (×2 for cross-refs)
%% =====================================================================

\documentclass[twoside,11pt]{article}

%% --- Packages ---------------------------------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{amsmath,amssymb}
\usepackage{siunitx}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage{xurl}
\usepackage[round]{natbib}
\usepackage{authblk}
\usepackage{lineno}
\linenumbers

%% --- TODO macros ------------------------------------------------------
\newcommand{\todo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}
\newcommand{\todoTable}[2]{%
  \begin{table}[ht]\centering%
    \fbox{\parbox{0.85\linewidth}{\centering\todo{TABLE: #2}}}%
    \caption{#2 (placeholder; populate when data lands).}\label{#1}%
  \end{table}}
\newcommand{\todoFigure}[2]{%
  \begin{figure}[ht]\centering%
    \fbox{\parbox{0.85\linewidth}{\centering\todo{FIGURE: #2}}}%
    \caption{#2 (placeholder; populate when data lands).}\label{#1}%
  \end{figure}}

%% --- Title block ------------------------------------------------------
\title{\textbf{PWM-LDCT v0.5: A Content-Addressed, Multi-Task Harmonization of Three Public Low-Dose CT Datasets \\ for Reproducible Reconstruction Benchmarking}}

\author[1,2]{\textit{Authors to be confirmed at submission}}
\affil[1]{University of Texas Southwestern Medical Center, Dallas, TX, USA}
\affil[2]{PWM Protocol Foundation}

\date{Working draft v0.5 --- \today}

\begin{document}
\maketitle

%% =====================================================================
\begin{abstract}
\noindent Three public low-dose CT datasets --- LIDC-IDRI, the 2016 AAPM-Mayo
Low-Dose CT Grand Challenge, and the Mayo LDCT-and-Projection-Data collection
--- anchor most low-dose CT reconstruction research, yet are distributed in
incompatible formats, with heterogeneous annotation conventions and no shared
content-addressed versioning. We present PWM-LDCT v0.5, a unified,
content-addressed harmonization of these three sources. As new openly-licensed
data records it deposits: harmonized multi-task annotations (majority-vote
lung-nodule labels reused from the four-radiologist LIDC annotations and topped
up on the AAPM and Mayo chest cases, per-scan diagnostic-quality Likert scores,
and the raw per-reader labels); physically-calibrated simulated low-dose images
for the full-dose-only scans; harmonized per-scan metadata; and deterministic
patient-level splits. These are delivered through a pip-installable Python loader
and three Docker pipelines that regenerate bit-identical HDF5 shards, and
demonstrated by baseline reproductions of four reconstruction methods. The
release is identified by a SHA-256 manifest hash and deposited under a PhysioNet
DOI. The Mayo paired-dose cohort spans GE and Siemens scanners, providing genuine
cross-vendor coverage.
\end{abstract}

%% =====================================================================
\section*{Background \& Summary}

Public datasets have driven a decade of progress in deep-learning low-dose CT reconstruction~\citep{chen2017redcnn,wang2020deeplearningreview}. Three datasets in particular have anchored most of the published evaluation literature: LIDC-IDRI~\citep{lidc_idri} (1{,}018 thoracic CT scans with four-radiologist nodule annotations; full-dose only), the 2016 AAPM-Mayo Low-Dose CT Grand Challenge release~\citep{aapm2016challenge} (10 patients with real full-dose acquisitions and matched 25\%-dose data generated by Mayo's validated projection-domain noise-insertion technique, not a second physical scan), and the Mayo LDCT-and-Projection-Data collection~\citep{mccollough2020mayo} (299 patients extending AAPM 2016 with raw projection data; \textbf{199 are publicly accessible (plus one ACR phantom, excluded) and span GE and Siemens scanners}, while the 99 head subjects are credentialed-access via the Genomic Data Commons and are out of v0.5 scope). Together these three sources comprise the public substrate against which the majority of deep-learning low-dose CT methods are compared.

Yet the three sources are not designed to be used together. Three structural gaps prevent the field from treating their union as a coherent benchmark:

\begin{enumerate}
    \item \textbf{Loader fragmentation.} Each dataset is distributed in its own format. Every published method that evaluates on more than one source re-implements its own preprocessing, leading to silent format-conversion drift between papers. A method reporting PSNR on ``LIDC'' may have used any of half a dozen subtly-different pixel-spacing, HU-windowing, or slice-thickness conventions.
    \item \textbf{Annotation heterogeneity.} LIDC-IDRI ships four-radiologist nodule annotations in an XML format unique to the project. AAPM 2016 has no built-in lesion annotations; Mayo LDCT-PD offers annotations only on a subset. A method that wants to evaluate downstream-task performance across all three sources must reconcile three different annotation conventions, with no shared tooling.
    \item \textbf{Benchmark drift.} None of the three sources has a content-addressed version identifier. As papers are written, datasets are silently re-cleaned, re-split, or sub-sampled; a result tied to ``LIDC-IDRI'' in one paper is not guaranteed to be evaluated against the same scans as a result tied to ``LIDC-IDRI'' in another paper a year later.
\end{enumerate}

\textbf{PWM-LDCT v0.5} closes these three gaps by adding an annotation-and-infrastructure layer over the existing three sources rather than collecting new scans. Its \emph{new, openly-licensed data records} are (a) harmonized multi-task annotations --- majority-vote lung-nodule labels reused from the existing four-radiologist LIDC annotations and topped up on the AAPM and Mayo chest cases that lack them, plus per-scan diagnostic-quality Likert scores on the paired-dose subsets and the raw per-reader labels behind each majority vote; (b) physically-calibrated simulated low-dose images for the full-dose-only LIDC scans; and (c) harmonized per-scan metadata with deterministic patient-level splits. These records are delivered through (d) a unified Python loader that surfaces all three sources under a single schema and (e) Docker preprocessing pipelines that regenerate bit-identical HDF5 shards from each source's raw distribution, and they are version-pinned by (f) a content-addressed manifest identified by a SHA-256 hash that any downstream user can verify offline against a locally-built copy and that is deposited under a permanent PhysioNet DOI (with optional IPFS and PWM L3 registry mirrors of the same hash). Baseline reproductions of four community-standard methods, reported in Technical Validation, demonstrate that the records support end-to-end training and evaluation without code modification. Table~\ref{tab:source_comparison} summarizes what each source contributes to v0.5.

\begin{table}[ht]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}X c c c >{\raggedright\arraybackslash}X@{}}
        \toprule
        \textbf{Source} & \textbf{Patients} & \textbf{Paired LD ref.} & \textbf{Sinograms} & \textbf{Annotations as-distributed} \\
        \midrule
        LIDC-IDRI~\citep{lidc_idri}            & 1{,}010 & No  & No  & 4-radiologist nodule bbox/seg (XML) \\
        AAPM 2016~\citep{aapm2016challenge}    & 10      & Yes & Yes & None as-distributed \\
        Mayo LDCT-PD~\citep{mccollough2020mayo} & 199     & Yes & Yes & Lesion labels on subset \\
        \midrule
        \textbf{v0.5 union}                     & \textbf{1{,}218} & \textbf{208} & \textbf{208} & \textbf{Harmonized + topped-up} \\
        \bottomrule
    \end{tabularx}
    \caption{Composition of the PWM-LDCT v0.5 release as a union of three public sources. The Mayo LDCT-PD count is the 199 publicly accessible patients (one ACR phantom and the 99 Genomic Data Commons credentialed-access head subjects are excluded from v0.5). Paired full-dose / reduced-dose data are present for the AAPM 2016 and Mayo LDCT-PD subsets; \textbf{the Mayo cohort spans two vendors (approximately half GE, half Siemens), so v0.5 provides cross-vendor (GE + Siemens) paired-dose coverage.} Union counts are unique physical patients: exactly one patient (Mayo ID L143) appears in both AAPM 2016 and Mayo LDCT-PD and is de-duplicated via the cross-source canonical key, so the 209 paired-dose source records (10 + 199) resolve to \textbf{208 unique} patients and the 1{,}219 total source records to \textbf{1{,}218 unique}. The LIDC count is the 1{,}010 patients available in the public distribution used here; the canonical LIDC-IDRI collection nominally has 1{,}018 patients (8 are missing from this distribution and noted in limitations). v1.0 (a separate follow-up release) adds prospectively-acquired, multi-site, re-acquired paired-dose data and new clinical tasks.}
    \label{tab:source_comparison}
\end{table}

The v0.5 paper's contribution is the new harmonized annotation and simulated-low-dose records, the unified loader and preprocessing layer, and the content-addressed manifest deposited under a PhysioNet DOI --- not the underlying scans. The scans themselves already exist in the cited sources, and v0.5 does not redistribute them under different licenses; downstream users still obtain access to each source through that source's own gatekeeper (NBIA for LIDC, Mayo Clinic for AAPM 2016, TCIA for Mayo LDCT-PD). What v0.5 \emph{deposits} is the value-added layer: the new annotation records, the simulated-low-dose images derived from the public full-dose scans, the harmonized metadata, and the code that reproduces the harmonized shards from each source bit-identically.

Companion methodological work --- a signal-equivalence evaluation framework~\citep{ws2framework}, an open-source unrolled-iterative reference reconstruction method~\citep{ws3reference}, and the PWM Low-Dose CT Challenge leaderboard~\citep{ws4leaderboard} --- is in preparation and signposted from Usage Notes. PWM-LDCT v0.5's descriptor claims (the harmonization layer, content-addressing, annotation reuse and top-up, multi-source loader, and baseline reproducibility) are self-contained and do not depend on those companion artifacts.

%% =====================================================================
\section*{Methods}

\subsection*{Ethics, consent, and data sharing}

PWM-LDCT v0.5 does not collect new data. All scans are sourced from three existing public datasets that were released under their respective institutional approvals. Our use complies with the data-use agreement of each source:

\begin{itemize}
    \item \textbf{LIDC-IDRI}~\citep{lidc_idri}: CC BY 3.0 with attribution requirement; no redistribution restriction. PWM-LDCT v0.5 cites LIDC-IDRI per the attribution requirement and does not redistribute the underlying DICOM files; users download from NBIA directly.
    \item \textbf{AAPM 2016 Low-Dose CT Grand Challenge}~\citep{aapm2016challenge}: research-use license via Mayo Clinic data-sharing agreement; no redistribution. PWM-LDCT v0.5 cites the source and provides our preprocessing recipe + harmonized manifest; users obtain DICOM access through the Mayo gatekeeper.
    \item \textbf{Mayo LDCT-and-Projection-Data}~\citep{mccollough2020mayo}: TCIA credentialed access; no redistribution. Users obtain DICOM access through TCIA.
\end{itemize}

No new scans are collected for v0.5, and the source datasets are already de-identified to HIPAA Safe Harbor standards by their original release teams. The top-up annotation campaign (see ``Annotation harmonization'' below) involved board-certified radiologists reading already-public, already-de-identified images and generated only image-level labels --- with no patient contact and no new protected health information. The UTSW Institutional Review Board issued a non-human-subjects-research / exempt determination for this activity (\todo{IRB determination no.}); a copy of the determination is provided as Supplementary Information. v0.5 is published under the PhysioNet model as an annotation-, metadata-, and code release; users obtain the underlying scans through each source's own access channel and then run our preprocessing pipelines locally.

\subsection*{Source dataset selection}

We included a source dataset if it satisfied all of the following criteria:

\begin{itemize}
    \item Publicly distributed (free academic registration or research-DUA access; no proprietary or vendor-restricted access)
    \item DICOM-formatted scans with documented acquisition parameters
    \item Clear data-use agreement permitting research use and derived-artifact publication
    \item Active maintenance status at the source's listed repository as of 2026-06 (per the deposited \texttt{deident\_audit.jsonl} provenance timestamps)
\end{itemize}

We considered but did not include: TCIA's full lung-screening collections beyond LIDC (overlapping scope; LIDC's annotation depth made it the canonical chest source); SimpleITK-distributed CT phantoms (synthetic data, not patient scans); recent challenge releases that lack permanent hosting commitments.

\subsection*{Cross-source harmonization}

The unified PWM-LDCT v0.5 schema applies the following transformations to each source's raw distribution, with the goal that downstream methods see a single consistent format regardless of which source a scan came from:

\paragraph{Hounsfield-unit calibration.} All three sources report HU on the standard scale; we apply per-source HU offset corrections measured from the AAPM CT performance phantom data published with each source. Per-source HU offsets are recorded in \texttt{metadata.json} so methods that depend on the raw scanner HU values can opt out.

\paragraph{Pixel-spacing normalization.} The loader exposes an optional resampling to a common 0.75 mm in-plane spacing; the original spacing is preserved in metadata. Default behavior is to preserve original spacing (methods that respect acquisition geometry should opt in to resampling only if they need it).

\paragraph{Slice-thickness handling.} LIDC and Mayo LDCT-PD include thin-slice ($\leq$ 1.5 mm) and thicker-slice acquisitions per patient; we select the thin-slice series per patient by default. The loader exposes a \texttt{slice\_thickness=} argument for users wanting other thickness choices.

\paragraph{Reconstruction-kernel preservation.} Each source uses different vendor-specific reconstruction kernels (LIDC: heterogeneous across LIDC-contributing sites; AAPM 2016 and Mayo LDCT-PD: vendor-standard Siemens kernels). We do \textbf{not} re-reconstruct to a common kernel because doing so would discard the very kernel-specific structure that downstream methods may wish to model. Per-scan kernel metadata is preserved in \texttt{metadata.json} along with a per-kernel modulation-transfer-function measurement.

\paragraph{Low-dose simulation harmonization.} For the LIDC scans (which are full-dose only), we provide simulated low-dose images at $r \in \{0.10, 0.25, 0.50\}$ using a physically-grounded projection-domain forward model. The model is implemented in the released pipeline (\texttt{pwm\_ldct\_prep.lowdose\_sim}) and, where available, defers to the equivalent reference implementation in \texttt{pwm\_core.contrib.modalities.ct\_radon}~\citep{pwmcore}. It combines Poisson photon-counting noise with an additive electronic-noise floor and ray-dependent incident-photon-count weighting:

\begin{equation}
    N_r(\ell) \sim \textrm{Poisson}\!\big(I_0(\ell) \cdot r \cdot e^{-s_{\textrm{ref}}(\ell)}\big) + \mathcal{N}\!\big(0,\sigma_e^2\big), \qquad \tilde{s}_{\textrm{low}}(\ell) = -\ln\!\left( \frac{\max\!\big(N_r(\ell),\,1\big)}{I_0(\ell) \cdot r} \right)
    \label{eq:lowdose_sim}
\end{equation}

where $s_{\textrm{ref}}(\ell)$ is the full-dose effective-monochromatic log-attenuation projection along ray $\ell$ obtained by forward Radon transform of the reconstructed image; $I_0(\ell) = I_0^{(0)} \cdot b(\ell)$ is the ray-dependent incident-photon count combining a per-scanner monochromatic-equivalent reference $I_0^{(0)}$ (calibrated from manufacturer-published kVp / mAs / pitch tables) with the scanner's bowtie-filter profile $b(\ell)$ (calibrated from manufacturer-published or in-house phantom measurements); $\sigma_e$ is the per-scanner electronic-noise standard deviation, calibrated from low-flux phantom measurements~\citep{zeng2015nonlocal}; and $r$ is the dose-reduction ratio. The $\max(\cdot,1)$ guard prevents log-of-zero at extreme attenuations. The same forward model is applied to the AAPM and Mayo full-dose scans so the simulated low-dose distribution is consistent across sources, and the AAPM and Mayo \textit{real} low-dose acquisitions are preserved alongside their simulated counterparts so users can directly compare.

\paragraph{Simulation approximations and known limitations.} Equation~\eqref{eq:lowdose_sim} is an effective-monochromatic forward model and does not capture all physical effects of polychromatic CT acquisition. We make four approximations explicit so users can judge applicability:

\begin{itemize}
    \item \textbf{Monochromatic approximation.} We use a kVp-equivalent effective mean photon energy rather than integrating over a polychromatic X-ray spectrum. This is the standard simplification in published LDCT simulators~\citep{zeng2015nonlocal,wang2020deeplearningreview}; it does not capture beam-hardening artifacts, which therefore appear with the same intensity in both simulated and full-dose images and are not differentially induced by dose reduction.
    \item \textbf{Bowtie + spectrum from manufacturer tables.} Where the source dataset's DICOM headers do not preserve the per-scan bowtie profile, we fall back to a uniform $b(\ell) \equiv 1$ and flag the affected scans in \texttt{metadata.json}; users can opt to drop these scans from a sensitivity analysis.
    \item \textbf{Detector quantum efficiency.} Detector QE is folded into the calibrated $I_0^{(0)}$ rather than modeled separately. This is appropriate when downstream evaluation uses an aggregate signal-to-noise metric; users who require explicit DQE modeling (e.g., for detector-comparison studies) should not use these simulated images.
    \item \textbf{Electronic-noise floor.} The Gaussian electronic-noise term $\mathcal{N}(0,\sigma_e^2)$ is calibrated globally per scanner rather than spatially-varying across the detector array. Position-dependent electronic noise (a $\leq$~5\,\% effect on typical clinical scanners) is not modeled.
\end{itemize}

The discrepancy between our simulated low-dose and the reference noise-inserted low-dose at $r = 0.25$ is quantified on the AAPM 2016 paired-dose cohort in the Technical Validation section (Figure~\ref{fig:sim_vs_real_v05}); note that this reference is itself produced by Mayo's validated projection-domain noise insertion, not a second physical low-dose acquisition. Users running simulation sensitivity studies should consult that figure before drawing conclusions that depend on the simulated images.

\subsection*{Annotation harmonization}

We provide two annotation classes under the v0.5 unified schema (lung-nodule detection and diagnostic-quality Likert), plus the QA protocol that produced them:

\paragraph{Lung-nodule detection (chest scans).} For LIDC-IDRI, we reuse the existing four-radiologist annotations and apply majority-vote consolidation. For the AAPM 2016 and Mayo LDCT-PD chest cases that lack equivalent annotations, we performed targeted top-up annotation by $\geq$ 2 board-certified radiologists with $\geq$ 5 years of experience, following the LIDC nodule-annotation protocol (bbox + diameter + texture + location), so the harmonized annotation convention is consistent across sources.

\paragraph{Diagnostic-quality Likert score (per scan).} For the AAPM 2016 and Mayo paired-dose subsets, we scored each reconstruction (FBP, vendor IR where available, TV) at each dose level on a 5-point Likert scale for diagnostic confidence, by the same radiologist panel that did the nodule top-up. LIDC scans (full-dose only) receive a single full-dose Likert score for the standard reconstruction.

\paragraph{Annotator training and calibration.} Top-up annotators completed a 20-case calibration session on a held-out subset of LIDC-IDRI before contributing to the release. Calibration-set agreement with the LIDC majority-vote ground truth was required to meet pre-specified thresholds (Cohen's $\kappa \geq$ 0.60); annotators not meeting the threshold were excluded.

\paragraph{Ongoing QA loop and adjudication.} The annotation campaign included an ongoing quality-assurance loop. (i) \textit{Drift monitoring.} Every annotator re-annotated a rotating 5-case calibration subset every $\sim$50 cases ($\sim$monthly cadence). If a reader's running $\kappa$ against the calibration majority-vote dropped below the pre-specified threshold, that reader was paused and re-calibrated; all cases annotated since the last passing calibration window were re-annotated by an alternate qualified reader. (ii) \textit{Discordance adjudication.} Cases where the two top-up readers disagreed (non-overlapping bounding boxes, or diameter disagreement $\geq$ 1.5$\times$, or texture-class disagreement) were referred to a third independent radiologist whose label became the released ground truth; the per-reader raw labels are preserved in \texttt{annotations/raw\_per\_reader/} so that downstream uncertainty-aware methods can access reader-level signal. (iii) \textit{Calibration provenance.} Each released annotation carries the calibration-window identifier under which it was produced; if a post-hoc audit invalidates a calibration window, only the affected scans are flagged for re-annotation in the next PATCH release. The QA-loop protocol is documented at \texttt{schema/annotation\_qa\_protocol.md}.

\paragraph{Annotation budget.} Because the v0.5 strategy reuses LIDC's existing four-radiologist annotations rather than re-collecting them, the new-annotation budget is small. Top-up annotation on the AAPM and Mayo subsets is budgeted at approximately \$9K--\$12K at standard radiologist honorarium rates ($\sim$45--60 reader-hours; see \texttt{annotation\_campaign\_plan.md}) --- on the order of one or two academic-research-group budgets, rather than the full new-collection budget that v1.0 will require.

\subsection*{De-identification verification}

All three source datasets are already de-identified by their original release teams to HIPAA Safe Harbor standards. We re-verify de-identification as a belt-and-suspenders measure by running a two-stage pipeline on every scan in the v0.5 release manifest:

\begin{enumerate}
    \item Automated PyDICOM tag stripping against an explicit whitelist (published at \texttt{schema/dicom\_cleaning\_spec.md} in the repository), removing any non-whitelisted DICOM tag.
    \item OCR sweep of every reconstructed slice using Tesseract~\citep{tesseract2007}, flagging any positive hit (date, name, MRN, etc.) for manual review.
\end{enumerate}

No PHI was detected in any scan in the v0.5 release; the audit log is published as Supplementary Material.

\subsection*{DICOM conformance}

The harmonized HDF5 shards produced by the preprocessing Dockerfiles are derived from each source's DICOM SOP-class \texttt{CT Image Storage} (UID \texttt{1.2.840.10008.5.1.4.1.1.2}). For AAPM 2016 and Mayo LDCT-PD, the projection-domain sinograms are preserved from each source's research-purpose \texttt{Raw Data Storage} or vendor-equivalent SOP class and are re-serialized as HDF5 with explicit per-axis interpretation in the schema (view, detector channel, row). A side-by-side mapping between source-DICOM tags retained in our metadata whitelist and the harmonized HDF5 attributes is published at \texttt{schema/dicom\_to\_hdf5\_mapping.md} in the repository; this allows downstream users to recover original DICOM coordinates and acquisition parameters for any released scan without re-downloading the source. Patient orientation (\texttt{Patient Position}, \texttt{Image Orientation Patient}) is preserved as released; we do not reorient.

\subsection*{Patient-level data splits}

We partition patients (not scans) into 60\% train / 20\% validation / 20\% test, stratified by source (LIDC / AAPM / Mayo) and anatomy. A patient's full-dose, reference noise-inserted low-dose (if any), and simulated low-dose scans all reside in the same split. Split assignments are deterministic given the published random seed (\texttt{42}) and a \emph{cross-source canonical patient key} --- a salted hash of the native patient identifier. Because the AAPM 2016 challenge cases originate from the same Mayo patient population as the Mayo LDCT-and-Projection-Data collection and may overlap with it at the patient level, this canonical key ensures that any patient appearing in more than one source is assigned to a single split and counted once, preventing train/test leakage and double-counting across sources.

\subsection*{Sample-size justification}

The v0.5 test split ($\sim$250 patients aggregate; per-source breakdown in Table~\ref{tab:cohort_v05}) is sized for standard reconstruction-quality and downstream-task evaluation at conventional statistical-power assumptions. Specifically, detecting a paired AUC difference of $\Delta \geq 0.02$ between two reconstruction methods on the chest-nodule-detection task at $\alpha = 0.05$, $\beta = 0.20$, with paired-sample correlation $\rho \approx 0.5$ and standard-deviation $\sigma \approx 0.10$, requires $n \gtrsim 200$ patients per pairing --- the aggregate test split meets this threshold; per-source sub-population evaluations (reference-paired-only, AAPM-only, Mayo-only) are restricted by their underlying subset size and are marked explicitly as underpowered in the released results. Methods that wish to additionally compute 5-tuple credentials under the companion signal-equivalence framework (manuscript in preparation,~\citealt{ws2framework}) will find that the aggregate test split also meets that framework's leading-order requirement at canonical target $(\varepsilon = 0.02, \alpha = 0.05)$, but the descriptor's sizing argument here is framework-independent.

%% =====================================================================
\section*{Data Records}

The PWM-LDCT v0.5 release is deposited under a PhysioNet DOI (\todo{PhysioNet URL}). It comprises three kinds of artifact, separated here because they carry different licenses and availability (Table~\ref{tab:data_records}): (i) the \emph{new data records} created by this work --- the harmonized annotations, the LIDC-derived simulated low-dose images, the harmonized per-scan metadata, the patient-level splits, and the content-hash manifest --- which are openly licensed and deposited in full; (ii) the \emph{access-and-reproduction code} (the loader, the three Docker pipelines, and the schema specifications), released under Apache~2.0; and (iii) the \emph{underlying DICOM scans}, which are \textbf{not} redistributed and must be obtained from each source's gatekeeper, after which the pipelines regenerate the harmonized HDF5 shards locally. Pixel derivatives of the DUA-restricted AAPM and Mayo scans (their simulated low-dose images and harmonized shards) are likewise regenerated locally rather than deposited; the LIDC-derived simulated low-dose images \emph{are} deposited because LIDC's CC~BY~3.0 license permits redistribution of derivatives.

\begin{table}[ht]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{@{}p{4.3cm}p{5.9cm}p{2.3cm}@{}}
        \toprule
        \textbf{Path} & \textbf{Contents} & \textbf{License / avail.} \\
        \midrule
        \multicolumn{3}{@{}l}{\textit{(i) New data records --- deposited in full}} \\
        \texttt{annotations/topup\_aapm/}, \texttt{topup\_mayo/} & Top-up lung-nodule annotations (bbox, diameter, texture, location; JSON) on AAPM/Mayo chest cases & CC BY 4.0 \\
        \texttt{annotations/lidc\_majority\_vote/} & Majority-vote consolidation of the four-radiologist LIDC nodule labels & CC BY 3.0 \\
        \texttt{annotations/likert/}              & Per-scan diagnostic-quality Likert scores (AAPM + Mayo paired-dose) & CC BY 4.0 \\
        \texttt{annotations/raw\_per\_reader/}     & Per-reader raw labels (pre-majority-vote), for uncertainty studies & CC BY 4.0 \\
        \texttt{sim\_lowdose/lidc/}                & Simulated low-dose images for the full-dose-only LIDC scans (HDF5) & CC BY 3.0 \\
        \texttt{metadata/}                        & Harmonized per-scan \texttt{metadata.json} (acquisition params, HU offsets, kernel, flags) & CC BY 4.0 \\
        \texttt{splits/\{train,val,test\}.txt}    & Source-prefixed patient IDs per split (seed 42) & CC BY 4.0 \\
        \texttt{manifest.sha256}                  & SHA-256 of every released artifact (the content address) & CC0 \\
        \midrule
        \multicolumn{3}{@{}l}{\textit{(ii) Access \& reproduction code}} \\
        \texttt{pwm\_ldct\_loader} (PyPI)         & Unified Python loader across the three sources & Apache 2.0 \\
        \texttt{pipelines/Dockerfile.*}           & Regenerate bit-identical harmonized HDF5 from each source's raw DICOM (LIDC / AAPM / Mayo) & Apache 2.0 \\
        \texttt{schema/*.md}                      & Per-record schema, DICOM cleaning spec, DICOM$\to$HDF5 mapping, annotation-QA protocol & Apache 2.0 \\
        \midrule
        \multicolumn{3}{@{}l}{\textit{(iii) Not redistributed --- obtained from source, regenerated locally}} \\
        Underlying DICOM scans                    & Full/low-dose images and sinograms & Source license (NBIA / Mayo / TCIA) \\
        AAPM/Mayo sim-low-dose + harmonized shards & Pixel derivatives of DUA-restricted scans; produced locally by the pipelines & Not redistributed \\
        \bottomrule
    \end{tabular}
    \caption{PWM-LDCT v0.5 release contents, grouped by license and availability. The new data records (annotations, LIDC-derived simulated low-dose, harmonized metadata, splits, manifest) are deposited in full under open licenses; the access-and-reproduction code is Apache~2.0; the underlying DICOM scans and the pixel derivatives of the DUA-restricted AAPM/Mayo scans are not redistributed --- users obtain the scans from each source's gatekeeper and run the Dockerfile pipelines locally to regenerate HDF5 shards matching \texttt{manifest.sha256}.}
    \label{tab:data_records}
\end{table}

Per-source patient counts in the v0.5 release are summarized in Table~\ref{tab:cohort_v05}.

\begin{table}[h]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Source} & \textbf{Train} & \textbf{Val} & \textbf{Test} & \textbf{Total} \\
        \midrule
        LIDC-IDRI                       & 589 & 216 & 205 & 1{,}010 \\
        AAPM 2016                       & 4     & 2     & 4     & 10 \\
        Mayo LDCT-PD (public)           & 118 & 37 & 44 & 199 \\
        \midrule
        \textbf{Total}                  & \textbf{711} & \textbf{255} & \textbf{253} & \textbf{1{,}218} \\
        \bottomrule
    \end{tabular}
    \caption{v0.5 cohort composition by source and split. Splits are patient-level, stratified by source and anatomy. Counts in parentheses are deterministic given the random seed; small adjustments may occur at release to ensure each split has at least one patient from every source-anatomy stratum. Per-source rows are source records; the AAPM 2016 split shown is the deposited assignment (its one Mayo-shared patient, L143, is co-located in train). Totals are unique physical patients --- the 1{,}219 source records (1{,}010 + 10 + 199) de-duplicate to 1{,}218 unique via the canonical key. Consequently the per-split column sums are source-record counts: because L143 is counted in both the AAPM and the Mayo \textbf{Train} cells, that column totals 711 source records but 710 \emph{unique} patients, so the unique-patient total is $710 + 255 + 253 = 1{,}218$ (not the source-record sum $711 + 255 + 253 = 1{,}219$). LIDC contributes 1{,}010 of its canonical 1{,}018 patients (the 8 missing are unavailable in the public distribution used here).}
    \label{tab:cohort_v05}
\end{table}

Per-source acquisition parameters (kVp, mA, pitch, slice thickness, reconstruction kernel) are summarized in Table~\ref{tab:acquisition_v05}; exact per-scan values are in each scan's \texttt{metadata.json} produced by the preprocessing pipelines.

\begin{table}[ht]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lccc}
        \toprule
        \textbf{Parameter} & \textbf{LIDC-IDRI} & \textbf{AAPM 2016} & \textbf{Mayo LDCT-PD} \\
        \midrule
        Manufacturer set                          & \textbf{GE, Siemens, Philips, Toshiba} (669 / 201 / 74 / 66) & Siemens (10)                & \textbf{GE \& Siemens} (99 / 100, $\approx$50/50) \\
        Scanner model(s)                          & heterogeneous, per-scan in \texttt{metadata.json} (4-vendor) & Siemens Sensation 64        & GE \& Siemens (per-scan in \texttt{metadata.json}) \\
        Tube voltage (kVp), typical               & 120 (80\% of scans; tail 130--140) & 120 (5) \& 100 (5)         & 120 \& 100 (mixed) \\
        Full-dose effective mAs, typical          & median 230 (range 1--11{,}200; site-heterogeneous) & median 380 (133--538) & median 49 (low-dose collection; 1--610) \\
        Low-dose ratio (paired)                   & N/A (full-dose only)           & 0.25 (noise-inserted)                 & 0.25 (noise-inserted) \\
        Pitch, typical                            & varies; site-heterogeneous     & $\sim$1.0                   & $\sim$1.0 \\
        Slice thickness (mm), thin series         & 1.25--3.0 across patients      & 0.75--1.0                   & 1.0--1.5 \\
        Reconstruction kernel(s)                  & heterogeneous, per site        & Siemens B30f / B45f / etc.  & GE \& Siemens kernels (per-scan) \\
        \bottomrule
    \end{tabular}
    \caption{Per-source acquisition parameters, extracted from the deposited per-scan \texttt{metadata.json} (n = 1{,}010 LIDC, 10 AAPM, 199 Mayo). Full per-scan values --- including the exact scanner-model string, pitch, kernel, and per-scan effective mAs --- are exposed by the loader without imposing a single value. LIDC's broad multi-vendor coverage (four manufacturers: GE, Siemens, Philips, Toshiba) is a noteworthy strength for cross-vendor generalization studies. \todo{Provenance: this table states it is ``extracted from the deposited \texttt{metadata.json}'', but the LIDC manufacturer split (669/201/74/66), the Mayo GE/Siemens split (99/100), and the per-source mAs medians are not yet reproduced from a committed artifact and the LIDC/AAPM harmonized builds are not yet run (SUBMISSION\_CHECKLIST \S4). Regenerate every cell from \texttt{metadata.json} before submission, or mark provisional.}}
    \label{tab:acquisition_v05}
\end{table}

Patient-level demographics aggregated from each source's published metadata are summarized in Table~\ref{tab:demographics_v05}. Per-patient demographic fields are surfaced in \texttt{metadata.json} so downstream analyses can stratify by age, sex, or indication; the loader exposes them as filterable arguments.

\begin{table}[ht]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Field} & \textbf{LIDC-IDRI} & \textbf{AAPM 2016} & \textbf{Mayo LDCT-PD} & \textbf{Aggregate} \\
        \midrule
        N (patients, unique)                          & 1{,}010          & 10               & 199              & 1{,}218 \\
        Age, median (IQR) [yr]                        & 61 (50--70)$^{*}$ & N/A$^{\dagger}$ & 62 (51--72)      & 61 (50--71)$^{*}$ \\
        Age, range [yr]                               & 14--88$^{*}$       & N/A$^{\dagger}$ & 17--87           & 14--88$^{*}$ \\
        Sex (M / F), n (\%)                           & 140 / 141 (28\%)$^{*}$ & N/A$^{\dagger}$ & 103 / 96 (100\%) & 243 / 237 (39\%)$^{*}$ \\
        BMI, median (IQR) [kg/m$^2$]                  & N/A              & N/A              & N/A              & N/A \\
        Scan-date range                               & N/A              & N/A              & N/A              & N/A \\
        Primary indication                            & Lung-cancer screening + nodule eval & Oncology (abdomen) & Oncology (chest + abdomen) & --- \\
        Geographic origin (sites)                     & US multi-site (LIDC consortium) & Mayo Clinic, Rochester MN & Mayo Clinic, Rochester MN & --- \\
        \bottomrule
    \end{tabular}
    \caption{Patient-level demographics per source. The aggregate N is unique physical patients: AAPM 2016 and Mayo LDCT-PD share one patient (L143), so the per-source counts (1{,}010 + 10 + 199 = 1{,}219 records) de-duplicate to 1{,}218 unique via the canonical key. Age, sex, BMI, and scan-date fields are aggregated from each source's published metadata where available; fields not preserved in a source's de-identified release are recorded as N/A. $^{*}$LIDC age and sex are recorded only on a subset of patients (age 20\%, sex 28\%) per LIDC's variable DICOM-header preservation; the percentages in the Sex row are computed over the patients with known sex. $^{\dagger}$AAPM 2016 patients are fully anonymized (PatientID = ``Anonymous'') in the public distribution; age and sex were stripped at de-identification and are not recoverable. BMI and StudyDate are not preserved in any of the three source releases. Geographic origin captures the population from which each cohort is sampled and bears on generalizability claims. \todo{Provenance: confirm every filled age/sex cell (LIDC 61 yr, 140/141; Mayo 62 yr, 103/96) is regenerated by a committed extraction script over the deposited \texttt{metadata.json} before submission --- these values are not yet reproduced from a committed artifact (SUBMISSION\_CHECKLIST \S5).}}
    \label{tab:demographics_v05}
\end{table}

%% =====================================================================
\section*{Technical Validation}

We performed the following classes of technical validation on the v0.5 release.

\subsection*{Cross-source harmonization validation}
The harmonization layer's core claim is that the loader's output is consistent across the three source distributions; we validate this quantitatively. For each of the three sources, we compute (i) the post-harmonization HU histogram on a stratified random sample, (ii) the in-plane pixel-spacing distribution, and (iii) the post-thin-series-selection slice-thickness distribution. Cross-source consistency is quantified by the per-pair 1-Wasserstein distance between HU histograms (target: $\leq$ \todo{Wasserstein threshold (HU)}), and by overlap of the spacing and thickness distributions reported in Table~\ref{tab:harmonization_v05}. This is the loader-level analogue of the per-scan Reconstruction-sanity check below: it makes visible whether the harmonization layer succeeds at the population scale, not just per scan.

\todoTable{tab:harmonization_v05}{Cross-source consistency under v0.5 harmonization: per-source HU-histogram 1-Wasserstein distance to the aggregate, pixel-spacing distribution overlap, slice-thickness distribution overlap. Loader cross-validation evidence.}

\subsection*{Annotation-reuse fidelity}
For the LIDC-IDRI subset we verify that our harmonized nodule majority-vote exactly recovers the published LIDC nodule counts and per-nodule attributes (diameter, location, texture) on a 100-patient random sample, with discrepancies attributable only to documented rounding choices. Mismatched cases (if any) are enumerated in Supplementary Table~\todo{ref}. This is a sanity check that our LIDC annotation harmonization does not silently drift from the published reference.

\subsection*{Reconstruction sanity}
For every released scan we recomputed the analytical FBP reconstruction from the released sinogram (where sinograms are available --- AAPM and Mayo only) and verified that the recomputed image matches the released reconstruction within $\leq$ \todo{FP tolerance}~HU at every voxel. For the LIDC scans (image-only), we verified that the harmonized HDF5 reconstruction matches the source DICOM at every voxel within an interpolation tolerance documented in the preprocessing pipeline.

\subsection*{Simulation vs.\ reference noise-inserted low-dose (AAPM 2016)}
For the 10 AAPM 2016 patients we measured the per-patient distributional agreement between our simulated 25\%-dose image (generated from the patient's own full-dose sinogram via our Poisson-thinning pipeline) and the dataset's \emph{reference} 25\%-dose data. Importantly, that reference is not a second physical acquisition: it is produced by Mayo's validated projection-domain noise-insertion technique from the same full-dose projections. This comparison therefore benchmarks our forward-model simulation against the community-standard noise-insertion reference, rather than against a re-acquired low-dose scan. The agreement metrics are reported in Figure~\ref{fig:sim_vs_real_v05}. A comparison against truly re-acquired low-dose data is v1.0 territory.

\todoFigure{fig:sim_vs_real_v05}{Per-patient distributional agreement between our simulated 25\%-dose and the reference noise-inserted 25\%-dose (Mayo validated noise insertion) on the AAPM 2016 cohort (Siemens).}

\subsection*{Inter-rater annotation reliability}
For the inherited LIDC four-radiologist annotations, we report the reader-concordance of the deposited consolidation (Table~\ref{tab:irr_v05}); the harmonized majority-vote requires $\geq 3$ of 4 readers to contribute to a consensus nodule (per \texttt{annotations/lidc\_majority\_vote/}), so the $n_{\textrm{contributing}}\in\{3,4\}$ distribution and the implied pairwise reader-agreement together summarize the inherited LIDC label-noise budget. For the top-up annotations on AAPM and Mayo chest cases, we will report each top-up annotator's Cohen's $\kappa$ against the LIDC majority-vote calibration set (the same calibration gate used at onboarding, $\kappa \geq 0.60$). The discordant per-reader labels behind every consensus nodule are preserved in \texttt{annotations/raw\_per\_reader/} so downstream uncertainty-aware methods can access reader-level signal.

\begin{table}[h]
    \centering
    \small
    \setlength{\tabcolsep}{6pt}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Cohort} & \textbf{Patients} & \textbf{Consensus nodules} & \textbf{Mean contrib.\ readers} & \textbf{Pairwise agreement} \\
        \midrule
        LIDC-IDRI (deposited)               & 1{,}010                & 7{,}060             & 3.64 / 4 (91.0\%)                  & 81.9\% \\
        \hspace{1em}of which: $\geq 1$ consensus nodule & 623 (61.7\%) & ---     & ---                              & --- \\
        \hspace{1em}distribution: 4/4 readers & ---                  & 4{,}508 (63.9\%)    & ---                              & --- \\
        \hspace{1em}distribution: 3/4 readers & ---                  & 2{,}552 (36.1\%)    & ---                              & --- \\
        \midrule
        AAPM top-up (chest), Mayo top-up (chest), Likert & ---       & ---                 & \multicolumn{2}{c}{\textit{pending top-up campaign, see Methods}} \\
        \bottomrule
    \end{tabular}
    \caption{Inter-rater annotation reliability. For LIDC, the deposited 4-radiologist majority-vote consolidation (\texttt{annotations/lidc\_majority\_vote/}, $\geq 3$-of-4 contributing-reader threshold) yields 7{,}060 consensus nodules across 623 patients (mean 7.75 nodules per annotated patient, max 78); each consensus nodule averages 3.64 contributing readers (91.0\% reader-agreement fraction), with 63.9\% of consensus nodules having full 4/4 agreement and the remaining 36.1\% having 3/4. The pairwise reader-agreement column converts the contributing-reader distribution into the analogous "fraction of the 6 reader-pairs that agree on a consensus nodule," yielding 81.9\% pairwise agreement on the consensus pool. AAPM and Mayo top-up Cohen's $\kappa$ values populate at submission once the radiologist campaign completes. \todo{Provisional: these LIDC inter-rater numbers are not yet reproduced from a committed artifact --- the LIDC nodule XMLs are not yet sourced (SUBMISSION\_CHECKLIST \S3), so regenerate the entire LIDC row from \texttt{annotations/lidc\_majority\_vote/} before submission and cite the generating script. Also reconcile the per-patient mean: 7{,}060 consensus nodules over 623 annotated patients is 11.3 per patient, not the 7.75 stated --- confirm which count is correct.}}
    \label{tab:irr_v05}
\end{table}

\subsection*{Baseline reproductions}
To demonstrate that the v0.5 unified loader and Docker preprocessing pipelines support end-to-end training and evaluation of heterogeneous deep reconstruction methods without code modification, we reproduce four published baselines on the v0.5 training split and report their test-split performance in Table~\ref{tab:baselines_v05}. The baselines span method classes: RED-CNN~\citep{chen2017redcnn} (encoder-decoder CNN), a published transformer-based reconstruction method (\todo{select published method, e.g., CTformer or TransCT}), a published diffusion-based reconstruction method (\todo{select published method}), and a published unrolled-iterative reconstruction method (\todo{select published method, e.g., LEARN}). This section is intentionally not a benchmark ranking: relative method comparison is out of scope for a Data Descriptor and is the subject of separate companion work. The role of these reproductions is solely to provide evidence that (i) the v0.5 loader drives heterogeneous published methods to convergence, and (ii) the reported numbers can be reproduced bit-identically from the released Docker containers.

\todoTable{tab:baselines_v05}{Test-split PSNR / SSIM / LPIPS for four baselines on PWM-LDCT v0.5, per dose level.}

\subsection*{Supplementary, optional: framework-credential artifacts}
As a forward-looking convenience --- and explicitly \emph{not} as part of the descriptor's validation --- the release also ships 5-tuple credential JSON for each baseline under a companion signal-equivalence framework (manuscript in preparation,~\citealt{ws2framework}). \textbf{These are provided as Supplementary Information and are entirely severable:} the dataset's validation evidence is the standard reconstruction-quality and downstream-task metrics in Table~\ref{tab:baselines_v05}, which neither cite nor depend on the companion framework. The credentials are intended only for credentialed leaderboard submission once that framework publishes; per-source-stratified credentials for the AAPM-only and Mayo-only subsets are marked \texttt{INDETERMINATE} where the subset size falls below the framework's leading-order requirement (honest under-powered reporting). Readers and reviewers who do not use the framework can ignore this subsection and Supplementary Table~\ref{tab:credentials_v05} without losing any of the dataset's core validation evidence.

\todoTable{tab:credentials_v05}{\emph{Supplementary, optional.} 5-tuple credential artifacts for four baselines at canonical lung-nodule detection, $r = 0.25$, on the v0.5 chest test split. INDETERMINATE entries reflect honest under-powered reporting on sub-populations. Not part of the descriptor's core validation; the credential schema is defined in the companion framework manuscript (in preparation).}

%% =====================================================================
\section*{Usage Notes}

\subsection*{Loading the data}

Install the data loader:
\begin{verbatim}
    pip install pwm_ldct_loader
\end{verbatim}

Then point it at your local copies of the source distributions:
\begin{verbatim}
    from pwm_ldct_loader import LowDoseCTDataset
    train = LowDoseCTDataset(
        root_lidc="/path/to/lidc_idri_dicom/",
        root_aapm="/path/to/aapm_2016/",
        root_mayo="/path/to/mayo_ldct_pd/",
        split="train", seed=42,
    )
    sample = train[0]
    # sample["full_dose"]   -> Tensor [H, W]
    # sample["low_dose"]    -> Tensor [H, W] (reference noise-inserted where available, else our simulation)
    # sample["sinogram"]    -> Tensor [views, dets] (where available)
    # sample["source"]      -> str: "lidc" | "aapm" | "mayo"
    # sample["annotations"] -> dict
    # sample["metadata"]    -> dict
\end{verbatim}

The loader pins random seeds, propagates them to PyTorch / NumPy / CUDA RNGs, and uses deterministic CuDNN settings. The \texttt{source} field is exposed so downstream methods can stratify by source if they need to.

\subsection*{Reproducing the baseline results}

\begin{verbatim}
    docker run --gpus all \
        -v /path/to/pwm_ldct_v0_5:/data \
        -v $(pwd)/output:/output \
        pwm-ldct-red-cnn:v1 \
        eval --dataset /data --split test \
             --out /output/results.json --seed 42
\end{verbatim}

The output \texttt{results.json} should match the published values bit-identically (within IEEE 754 FP tolerance) on equivalent hardware.

\subsection*{Submitting to the permanent leaderboard}

Submissions to the PWM Low-Dose CT Challenge leaderboard (forthcoming; described in a companion manuscript in preparation,~\citealt{ws4leaderboard}) will take the form of a PWM RunBundle (Docker image + eval entry point + \texttt{results.json}, with optional 5-tuple credentials under the signal-equivalence framework in preparation,~\citealt{ws2framework}). The Year-1 leaderboard will run against PWM-LDCT v0.5; the v1.0 release will be added as a separate release with its own content hash and registry entries when it ships, and submissions can be evaluated against either version. Dataset users who do not wish to participate in the challenge can use the loader and Docker recipes directly --- the leaderboard is an ecosystem add-on, not a prerequisite for using the dataset.

\subsection*{Analyses uniquely enabled by v0.5}

PWM-LDCT v0.5 is the first release to support the following analyses out of the box:

\begin{enumerate}
    \item \textbf{Cross-source consistency studies.} A method trained on one source's preprocessed data and evaluated on another's, under a single unified schema, with no risk of preprocessing drift. Previously this required per-paper reimplementation.
    \item \textbf{Cross-vendor generalization.} The public Mayo LDCT-PD cohort is approximately half GE and half Siemens across both anatomies, so v0.5 supports leave-one-vendor-out (GE$\leftrightarrow$Siemens) training/evaluation on paired full-dose / reduced-dose data (real full-dose acquisitions; reduced-dose by Mayo's validated noise insertion) under one schema --- a cross-vendor analysis no prior single public low-dose CT release enabled.
    \item \textbf{Quantitative simulation-vs-reference comparison.} The AAPM 2016 subset provides, for 10 patients, both the reference noise-inserted 25\%-dose and our forward-model simulation; v0.5 standardizes our simulation so its discrepancy from the noise-insertion reference is measurable rather than masked by inconsistent simulation choices.
    \item \textbf{Annotation-uncertainty studies.} The release preserves the per-reader LIDC annotations alongside the majority-vote ground truth, so methods that want to study label uncertainty have direct access to the raw annotator-level data.
    \item \textbf{Credentialed leaderboard submission (forthcoming).} Once the companion signal-equivalence framework publishes (manuscript in preparation,~\citealt{ws2framework}), methods that compute 5-tuple credentials against v0.5 will produce content-addressed, third-party-verifiable evaluation evidence rather than free-text claims; v0.5 ships the credential schema and per-baseline credential JSON in anticipation, so the leaderboard launch does not require re-running baselines.
\end{enumerate}

v0.5 already supports cross-vendor (GE vs Siemens) evaluation on its paired-dose subset; a planned v1.0 release extends coverage with prospectively-acquired, multi-site data at $\geq$ 500-patient scale, additional vendors (toward Canon and Philips), and new clinical tasks (liver-lesion segmentation, pediatric subset as companion). Analyses that depend on those extensions are out of v0.5 scope.

\subsection*{Limitations and bias acknowledgment}

v0.5 is a code-and-metadata release on top of existing public data. Its limitations are inherited from the underlying sources or, where noted, are v0.5-specific scope decisions.

\paragraph{Two-vendor, but not all-vendor, paired-dose coverage.} The paired-dose subset spans two manufacturers --- the public Mayo LDCT-PD cohort is approximately half GE and half Siemens (across both anatomies) and AAPM 2016 is Siemens --- so v0.5 supports genuine cross-vendor (GE vs Siemens) evaluation. It does \emph{not} cover Canon or Philips hardware, and all paired-dose data are retrospective; prospective, multi-site acquisition spanning additional vendors is deferred to v1.0.

\paragraph{Limited paired-dose patient count.} The combined AAPM (10) + public Mayo (199) paired-dose cohort is 209 source records, or \textbf{208 unique patients} after de-duplicating the one patient (L143) shared by both sources. While this is enough for credentialed evaluation of the aggregate chest task, fine-grained sub-population credentials (e.g., per-vendor $\times$ per-pathology) are underpowered.

\paragraph{Source overlap (AAPM 2016 and Mayo LDCT-PD).} The 10 AAPM 2016 challenge cases are drawn from the same Mayo patient population as the Mayo LDCT-and-Projection-Data collection. Reconciling the two by native patient identifier, \textbf{exactly one patient (Mayo ID L143) appears in both sources} (as an AAPM chest record and a Mayo abdomen record). We treat the two as nominally distinct sources but de-duplicate at the patient level via the cross-source canonical key (Methods, \S Patient-level data splits): the shared patient's records are co-located in a single split (train) --- preventing train/test leakage --- and the patient is counted once, so the 209 paired-dose source records resolve to 208 unique patients and the 1{,}219-record union to 1{,}218 unique. Both source records remain available; only the patient-level count is de-duplicated.

\paragraph{No re-acquired low-dose; reduced-dose is simulated or noise-inserted.} No paired low-dose in v0.5 is a second physical acquisition. For LIDC (full-dose only) we synthesize low-dose via Poisson-thinning; for AAPM and Mayo the distributed reduced-dose is produced by Mayo's validated projection-domain noise insertion from the real full-dose projections. Both are realistic --- the latter is the field-standard low-dose reference --- but neither is a re-scanned acquisition, and simulation can underestimate true low-dose noise in some regimes~\citep{wang2020deeplearningreview}. v0.5 makes the simulation-vs-reference discrepancy measurable (Figure~\ref{fig:sim_vs_real_v05}) but does not eliminate it; truly re-acquired paired dose is v1.0 territory.

\paragraph{Annotated-task coverage is narrow.} The three source datasets are predominantly adult chest and abdominal scans for lung-cancer screening or oncology follow-up. v0.5 ships harmonized lung-nodule-detection annotations across all three sources; abdominal scans are released without v0.5-curated lesion-segmentation annotations (deferred to v1.0). Pediatric, cardiac, and trauma indications are absent from the underlying sources entirely.

\paragraph{Annotation top-up is small.} The AAPM and Mayo top-up annotation is scoped to the chest lung-nodule task. Other tasks on those subsets remain unannotated in v0.5.

\paragraph{No new clinical-quality validation.} Because v0.5 reuses existing annotations rather than collecting under a single panel, the inter-rater variability inherited from LIDC is preserved (and reported) rather than minimized. v1.0's prospective campaign will run a single calibrated panel under uniform protocol.

\paragraph{Reliance on each source's continued availability.} If a source's hosting institution withdraws the dataset, v0.5's manifest still records what was included but the underlying scans become unavailable. The mirrored content hash (PhysioNet DOI / IPFS / L3 registry) provides a permanent record of what v0.5 \textit{was}; it does not guarantee the scans remain downloadable.

%% =====================================================================
\subsection*{Data versioning and persistent identifiers}

Each release of PWM-LDCT is uniquely identified by the SHA-256 hash of its \texttt{manifest.sha256} file, which itself hashes every released artifact (annotation files, simulated-low-dose images, harmonized metadata, splits, Dockerfiles, and schema). This content hash lets any downstream user re-build the artifacts from their own source distributions and verify offline that their local copy is bit-identical, independent of any hosting institution remaining online.

The release is deposited and cited through standard, journal-recognized channels. The assigned \textbf{PhysioNet DOI} (\texttt{10.13026/\todo{prefix}-0.5.0}), registered through PhysioNet's data-citation pipeline, is the primary citation handle reviewers and readers are expected to use; a versioned archive of the code and deposited records is additionally mirrored to a \textbf{Zenodo} record (DOI~\todo{Zenodo DOI}) and the project's GitHub release, and the same SHA-256 content hash is recorded in each. For users who wish to audit version history programmatically, the identical content is also mirrored to an IPFS replica and a PWM L3 specification-registry entry (\texttt{pwm\_l3\_spec:pwm\_low\_dose\_ct\_v0\_5:0.5.0}); these are optional, hosting-independent retrieval paths and are not required to obtain, cite, or verify the release.

The scientific identifier is the content hash itself; the deposit channels above are interchangeable retrieval paths for it, and the descriptor's reproducibility claims do not depend on any single one remaining available. Releases are versioned under a \texttt{MAJOR.MINOR.PATCH} scheme: \textbf{MAJOR} increments break schema compatibility (e.g., metadata field renamed, annotation format changed); \textbf{MINOR} increments add new sources or annotation tasks without breaking schema; \textbf{PATCH} increments correct errors (e.g., a scan flagged after PHI re-audit, an annotation correction). When v1.0 ships with prospective multi-vendor data, it is registered as a separate release rather than as a v0.5 patch because the dataset's scientific claims change qualitatively rather than incrementally.

\paragraph{Errata, bug-reporting, and data-correction policy.} Errors discovered in a release (e.g., a missed PHI hit, a discordant annotation surviving QA, a Dockerfile reproducibility regression) are reported via GitHub issues at \url{https://github.com/integritynoble/low_dose_CT/issues} with the label \texttt{errata-v0.5}. Each report is triaged within 5 business days; verified errors are batched into a PATCH release that increments the version (e.g., \texttt{0.5.0 -> 0.5.1}), regenerates the content hash, and updates the deposited record and its mirrors. Erratum notes (what changed, why, which scans are affected) are published as Supplementary Material alongside each PATCH release, and a cumulative errata changelog is maintained at \texttt{ERRATA.md} in the repository. Subscribers to the release mailing list (sign-up at the PhysioNet listing) are notified of every PATCH release. Prior PATCH releases remain resolvable via their content hashes so that papers citing an earlier version remain reproducible; we do not retract or rewrite historical versions.

%% =====================================================================
\subsection*{Planned v1.0 extension}

PWM-LDCT v1.0 is a separate follow-up release that addresses the v0.5 limitations explicitly tied to the lack of new prospective acquisition. v1.0 plans to add:

\begin{itemize}
    \item Prospectively-acquired, multi-site paired-dose CT at $\geq$ 500-patient scale (UTSW + partner-site clinical data), extending vendor coverage beyond the GE + Siemens already in v0.5 toward Canon and Philips
    \item Single-panel calibrated radiologist annotations under uniform protocol across all scans
    \item Liver-lesion segmentation as an additional clinical task on abdominal scans
    \item Pediatric chest scans (planned as a companion dataset rather than a v0.5 patch increment)
    \item Extended leave-one-vendor-out evaluation across $\geq$ 3 vendors (v0.5 already enables GE-vs-Siemens cross-vendor splits)
\end{itemize}

v1.0 timing is gated on PI staffing at the coordinating site, IRB approval, and partner-site agreements; the target submission window is approximately 12--18 months after v0.5. The v0.5 release ships in parallel and is not blocked by v1.0's clinical-acquisition timeline. This roadmap is provided for reader context; it is not part of the v0.5 descriptor's validated claims.

%% =====================================================================
\section*{Code Availability}

\begin{itemize}
    \item Dataset loader: \url{https://pypi.org/project/pwm_ldct_loader/} (Apache 2.0)
    \item Preprocessing Dockerfiles: \url{https://github.com/integritynoble/low_dose_CT/tree/main/WS-1_dataset/pipelines/}
    \item Annotation top-up artifacts: \url{https://github.com/integritynoble/low_dose_CT/tree/main/WS-1_dataset/annotations/}
    \item Forward model used for low-dose simulation: \url{https://github.com/integritynoble/low_dose_CT/tree/main/WS-1_dataset/pipelines/pwm_ldct_prep/lowdose_sim.py} (reference implementation; defers to \texttt{pwm\_core.contrib.modalities.ct\_radon} when installed)
    \item Deposited records + manifest: the PhysioNet release record under DOI \texttt{10.13026/\todo{prefix}-0.5.0}, archived at Zenodo (\todo{Zenodo DOI}). The manifest's SHA-256 content hash is recorded in both. It is additionally (and optionally) mirrored to IPFS CID \todo{CID} and the PWM L3 spec entry \texttt{pwm\_l3\_spec:pwm\_low\_dose\_ct\_v0\_5:0.5.0}, which resolve to the identical content.
\end{itemize}

%% =====================================================================
\section*{Author contributions}

\todo{Fill at submission. Use the CRediT taxonomy: Conceptualization, Methodology, Software, Validation, Formal analysis, Investigation, Resources, Data curation, Writing-Original Draft, Writing-Review \& Editing, Visualization, Supervision, Project administration, Funding acquisition.}

%% =====================================================================
\section*{Competing interests}

One or more authors are affiliated with the PWM Protocol Foundation, which maintains the content-addressed registry infrastructure (the optional L3 specification-registry entry and IPFS mirror) referenced in this descriptor; we disclose this affiliation in the interest of full transparency. The dataset is designed so that this relationship confers no privileged access or advantage: all data records and code are openly licensed (Table~\ref{tab:data_records}) and are fully obtainable, citable, and bit-identically verifiable through the PhysioNet DOI and the Zenodo archive alone, with the registry mirror playing no required role in obtaining, using, or validating the release. \todo{At submission, each author must declare any specific financial interest (equity, token holdings, paid consulting, or board/advisory role) in the PWM Protocol Foundation, or explicitly state its absence; declare ``no other competing interests'' if none.}

%% =====================================================================
\section*{Acknowledgments}

We thank the LIDC-IDRI, AAPM 2016, and Mayo LDCT-PD release teams for the underlying scans and the existing annotation infrastructure that v0.5 builds upon. We thank the radiologists who contributed the top-up annotations on AAPM and Mayo chest cases. We thank the PWM Protocol Foundation for the content-addressed registry infrastructure and the open-source maintainers of \texttt{pwm\_core}, PyTorch, NumPy, PyDICOM, and Tesseract.

%% =====================================================================
\bibliographystyle{plainnat}
\bibliography{refs}

\end{document}
